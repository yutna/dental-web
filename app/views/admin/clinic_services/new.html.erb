<% content_for :title, t("admin.clinic_services.new") %>

<main class="mx-auto w-full max-w-3xl space-y-5 px-6 py-10">
  <header>
    <h1 class="text-2xl font-semibold text-app-text-primary"><%= t("admin.clinic_services.new") %></h1>
    <p class="mt-1 text-sm text-app-text-secondary"><%= t("admin.clinic_services.new_description") %></p>
  </header>

  <section class="rounded-2xl border border-app-border-primary bg-app-surface-primary p-6 shadow-sm">
    <%= render "form", clinic_service: @clinic_service %>
  </section>
</main>
